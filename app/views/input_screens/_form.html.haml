= form_for @input_screen, html: { class: "form-horizontal", role: "form" } do |f|
  -if @input_screen.errors.any?
    .alert.alert-danger.alert-dismissable{role: "alert"}
      %button.close{type: "button", data: {dismiss: "alert"} }
        %span{aria: {hidden: "true"} } &times;
        %span.sr-only Close
      %h4= "#{pluralize(@input_screen.errors.count, "error")} prohibited this input_screen from being saved:"

      %ul
        - @input_screen.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :name, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :name, class: 'form-control'
  .form-group
    = f.label :navigation_label, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :navigation_label, class: 'form-control'
  .form-group
    = f.label :identifier, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :identifier, class: 'form-control'
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: 'btn btn-primary'
